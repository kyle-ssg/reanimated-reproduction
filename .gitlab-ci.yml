
stages:
    - test
    #- deploy

test:
    image: lakoo/node-alpine-gcloud
    stage: test
    script:
        - npm i
        #        - npm run test:e2e
        - npm run test:pact
        - npm run test:pact:publish
    only:
        - develop
        - master

#deploy:
#    image: ilyasemenov/gitlab-ci-git-push
#    stage: deploy
#    script:
#        - git-push dokku@dokku1.solidstategroup.com:app-name
#    only:
#        - master
